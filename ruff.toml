target-version = "py310"
extend-exclude = [".release", ".venv", "_tmp"]

[lint]
# Start the mandatory gate with correctness rules that catch syntax errors,
# undefined names and invalid control flow without turning legacy style into a
# one-time repository-wide rewrite.
select = ["E9", "F63", "F7", "F82"]
