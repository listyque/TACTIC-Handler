# TACTIC-Handler UI shell

This package is the canonical PySide6/QML application interface and composition root.

Run the shell after installing PySide6:

```powershell
py -m pip install PySide6
py launch.py
```
